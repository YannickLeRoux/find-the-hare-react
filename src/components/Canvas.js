import React from 'react';

const Canvas = () => {
  return (
    <React.Fragment>
      <canvas className="backgroundcanvas" width="1000" height="1000">Error, canvas is not supported</canvas>
    </React.Fragment>

  );

};

export default Canvas;